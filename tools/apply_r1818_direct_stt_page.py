from pathlib import Path

CLIENT = Path('app/src/main/java/com/oai/geminilivetranslate/network/AiStudioFileTranscribeClient.kt')
EXEC = Path('app/src/main/java/com/oai/geminilivetranslate/core/AiStudioWebSessionExecutor.kt')
BRIDGE = Path('app/src/main/java/com/oai/geminilivetranslate/ui/AiStudioSttPageBridge.kt')
TEST = Path('app/src/test/java/com/oai/geminilivetranslate/network/AiStudioDirectSttPageSourceTest.kt')

client = CLIENT.read_text()
client = client.replace('''        val exec = createAndAwaitReady()\n        selectModel(exec)\n        onProgress("Đang đưa tệp vào AI Studio và chờ trang đọc xong...", 8)\n        attachAndWait(exec, uri, displayName, mimeType, size)\n        logger.log(2, TAG, "CONFIG model=$model prompt=false autoLanguage=true diarizationRequested=$speakerDiarization transport=aistudio-web-file-only manualRun=false autoSubmit=true")\n''','''        val exec = createAndAwaitReady()\n        onProgress("Đang đưa tệp vào trang chép lời AI Studio và chờ trang đọc xong...", 8)\n        attachAndWait(exec, uri, displayName, mimeType, size)\n        logger.log(2, TAG, "CONFIG model=$model prompt=false autoLanguage=true diarizationRequested=$speakerDiarization transport=aistudio-stt-direct-page manualRun=false autoSubmit=true")\n''')
client = client.replace('''            holder.set(created); executor = created; created.start()\n''','''            holder.set(created); executor = created; created.startFileTranscribe(model)\n''')
start = client.find('''    private fun selectModel(exec: AiStudioWebSessionExecutor) {''')
end = client.find('''    private fun attachAndWait''', start)
if start < 0 or end < 0:
    raise SystemExit('selectModel block not found')
client = client[:start] + client[end:]
client = client.replace('''            exec.attachFile(uri, name, mime, size, requireUploadReady = true) { yes, d ->''','''            exec.attachSttFile(uri, name, mime, size) { yes, d ->''')
client = client.replace('''            val accepted = exec.generateAttachmentFileOnlyNative { r -> ref.set(r); latch.countDown() }''','''            val accepted = exec.generateSttFileNative { r -> ref.set(r); latch.countDown() }''')
client = client.replace('''name.startsWith("R24_") || name.startsWith("JS_R24_")''','''name.startsWith("R29_") || name.startsWith("JS_R29_") || name.startsWith("R28_") || name.startsWith("JS_R28_") || name.startsWith("R27_") || name.startsWith("JS_R27_") || name.startsWith("R26_") || name.startsWith("JS_R26_") || name.startsWith("R25_") || name.startsWith("JS_R25_") || name.startsWith("R24_") || name.startsWith("JS_R24_")''')
CLIENT.write_text(client)

bridge = r'''package com.oai.geminilivetranslate.ui

/** Dedicated bridge for AI Studio's Speech-to-Text surface opened with the transcribe model URL. */
object AiStudioSttPageBridge {
    const val VERSION = "2026-09-05-stt-page-r1-direct-model"

    val DOCUMENT_START: String = """
        (function() {
          'use strict';
          if (window.__AIS_STT_PAGE__ && window.__AIS_STT_PAGE__.version === '$VERSION') return;
          const state={fileChooserServed:false,fileName:'',fileMime:'',fileSize:-1,fileServedAt:0,baselineLines:null,runStartedAt:0};
          function emit(kind,payload){try{if(window.AIStudioWebSessionLab&&window.AIStudioWebSessionLab.onJsEvent)window.AIStudioWebSessionLab.onJsEvent(JSON.stringify({t:Date.now(),kind:kind,payload:payload||{}}));}catch(_){}}
          function visible(el){try{if(!el||!el.isConnected)return false;const r=el.getBoundingClientRect(),s=getComputedStyle(el);return r.width>=1&&r.height>=1&&s.display!=='none'&&s.visibility!=='hidden';}catch(_){return false;}}
          function txt(el){try{return [el.textContent||'',el.getAttribute&&el.getAttribute('aria-label')||'',el.getAttribute&&el.getAttribute('title')||''].join(' ').replace(/\\s+/g,' ').trim();}catch(_){return '';}}
          function model(raw){return String(raw||'').trim().replace(/^models\\//i,'').slice(0,140);}
          function surface(){let m='';try{m=model(new URL(location.href).searchParams.get('model'));}catch(_){}const zero=!!document.querySelector('ms-stt-zero-state');const input=!!document.querySelector('input[data-test-upload-file-input],input[type="file"][accept*="audio"]');let title=false;try{title=/turn speech to transcriptions/i.test(String(document.body&&document.body.innerText||'').slice(0,120000));}catch(_){}return{urlModel:m,zeroState:zero,audioInput:input,title:title,stt:zero||input||title};}
          function runButton(){let nodes=[];try{nodes=document.querySelectorAll('button,[role="button"]');}catch(_){}let best=null,score=-9999;for(let i=0;i<nodes.length&&i<2200;i++){const el=nodes[i];if(!visible(el))continue;const t=txt(el);let s=0;if(/^(run|chạy)$/i.test(t))s+=5000;else if(/\\b(run|chạy)\\b/i.test(t))s+=1800;if(String(el.tagName||'')==='BUTTON')s+=300;if(/upload|record|drive|settings|tune|search|mic|microphone/i.test(t))s-=5000;if(el.disabled||(el.getAttribute&&el.getAttribute('aria-disabled')==='true'))s-=2500;if(s>score){best=el;score=s;}}return{el:best,score:score};}
          function point(el){if(!el)return{ok:false,error:'NO_TARGET'};try{const r=el.getBoundingClientRect(),vw=Math.max(1,innerWidth),vh=Math.max(1,innerHeight),x=Math.min(vw-1,Math.max(0,r.left+r.width*.5)),y=Math.min(vh-1,Math.max(0,r.top+r.height*.5)),hit=document.elementFromPoint(x,y),ok=!!(hit&&(hit===el||el.contains(hit)));return{ok:ok,error:ok?'':'TARGET_COVERED',xRatio:x/vw,yRatio:y/vh,label:txt(el).slice(0,160),hit:hit?{tag:String(hit.tagName||''),label:txt(hit).slice(0,160)}:null};}catch(e){return{ok:false,error:String(e)}}}
          function clean(s){s=String(s||'').replace(/\\s+/g,' ').trim();if(!s)return'';if(/^(turn speech to transcriptions|record or provide audio.*|click or drop file here|audio files only|upload|drive|record audio|run|playground)$/i.test(s))return'';return s;}
          function captureBaseline(){const set=new Set();try{String(document.body&&document.body.innerText||'').split(/\\r?\\n/).forEach(function(x){x=clean(x);if(x)set.add(x);});}catch(_){}state.baselineLines=set;}
          function resultText(){const candidates=[];let nodes=[];try{nodes=document.querySelectorAll('ms-stt-transcription,ms-transcription,[data-test-id*="transcript"],[data-testid*="transcript"],[class*="transcript"],[class*="transcription"],[aria-label*="transcript"],ms-chat-turn,[class*="response"]');}catch(_){}for(let i=0;i<nodes.length&&i<1800;i++){if(!visible(nodes[i]))continue;const t=clean(nodes[i].innerText||nodes[i].textContent||'');if(t.length>=2)candidates.push({t:t,s:t.length+500});}try{const base=state.baselineLines||new Set();String(document.body&&document.body.innerText||'').split(/\\r?\\n/).forEach(function(line){const t=clean(line);if(t.length>=2&&!base.has(t))candidates.push({t:t,s:t.length});});}catch(_){}candidates.sort(function(a,b){return b.s-a.s});return candidates.length?candidates[0].t:'';}
          const api={version:'$VERSION',
            pageState:function(expected){const s=surface(),e=model(expected),modelOk=!e||!s.urlModel||s.urlModel===e,out={ok:true,ready:!!(s.stt&&modelOk),expectedModel:e,urlModel:s.urlModel,sttSurface:s.stt,zeroState:s.zeroState,audioInput:s.audioInput,title:s.title,path:String(location.pathname||'').slice(0,300)};emit('R28_STT_PAGE_STATE',out);return out;},
            uploadTarget:function(){let nodes=[];try{nodes=document.querySelectorAll('button.upload-file-menu-item,button[aria-label="Upload"],button,[role="button"]');}catch(_){}let best=null,score=-9999;for(let i=0;i<nodes.length&&i<1800;i++){const el=nodes[i];if(!visible(el))continue;const t=txt(el);let s=0;if(el.matches&&el.matches('button.upload-file-menu-item'))s+=6000;if(/^upload$/i.test(t))s+=4500;else if(/\\bupload\\b/i.test(t))s+=1800;if(/record|drive|run|search/i.test(t))s-=3500;if(s>score){best=el;score=s;}}const out=point(best);out.score=score;emit('R28_STT_UPLOAD_TARGET',{ok:out.ok,error:out.error||'',score:score,label:out.label||'',hit:out.hit||null});return out;},
            markFileChooserServed:function(name,mime,size){state.fileChooserServed=true;state.fileName=String(name||'').slice(0,260);state.fileMime=String(mime||'').slice(0,180);state.fileSize=Number(size||-1);state.fileServedAt=Date.now();const out={ok:true,name:state.fileName,mime:state.fileMime,size:state.fileSize};emit('R28_STT_FILE_CHOOSER_SERVED',out);return out;},
            fileState:function(expectedName){const name=String(expectedName||state.fileName||'');let nameVisible=false,busy=false;try{const body=String(document.body&&document.body.innerText||'').slice(-180000);nameVisible=!!name&&body.indexOf(name)>=0;busy=/uploading|processing|preparing|đang\\s*tải|đang\\s*xử\\s*lý/i.test(body.slice(-40000));}catch(_){}const r=runButton(),disabled=!!(r.el&&(r.el.disabled||(r.el.getAttribute&&r.el.getAttribute('aria-disabled')==='true'))),runReady=!!(r.el&&r.score>=1500&&!disabled),settled=state.fileChooserServed&&Date.now()-state.fileServedAt>=700,ready=!!(state.fileChooserServed&&runReady&&!busy&&(nameVisible||settled)),out={ok:true,present:state.fileChooserServed,ready:ready,busy:busy,nameVisible:nameVisible,runReady:runReady,runScore:r.score,runLabel:r.el?txt(r.el).slice(0,160):'',servedMs:state.fileChooserServed?Date.now()-state.fileServedAt:-1};emit('R28_STT_FILE_STATE',out);return out;},
            runTarget:function(){const r=runButton(),out=point(r.el);out.score=r.score;let base=-1;try{base=Number(window.__AIS_WEB_SESSION__&&window.__AIS_WEB_SESSION__.captureCount||0);}catch(_){}out.baselineCaptureCount=base;captureBaseline();state.runStartedAt=Date.now();emit('R28_STT_RUN_TARGET',{ok:out.ok,error:out.error||'',score:r.score,label:out.label||'',baselineCaptureCount:base,hit:out.hit||null});return out;},
            resultState:function(){const t=resultText();let net=null,status=-1,responseChars=0,partial=true,netOk=false;try{net=window.__AIS_WEB_SESSION__&&window.__AIS_WEB_SESSION__.getLastSafeResponse?window.__AIS_WEB_SESSION__.getLastSafeResponse():null;if(net){status=Number(net.status||-1);responseChars=Number(net.responseChars||0);partial=!!net.partial;netOk=!!net.ok;}}catch(_){}const out={ok:true,text:t,textChars:t.length,source:t?'dom':'',status:status,responseChars:responseChars,partial:partial,networkOk:netOk,terminal:!!(net&&netOk&&!partial),elapsedMs:state.runStartedAt?Date.now()-state.runStartedAt:-1};emit('R28_STT_RESULT_STATE',{ok:true,textChars:out.textChars,source:out.source,status:status,responseChars:responseChars,partial:partial,networkOk:netOk,terminal:out.terminal,elapsedMs:out.elapsedMs});return out;}
          };
          window.__AIS_STT_PAGE__=api;emit('R28_STT_BRIDGE_INSTALLED',{version:api.version,path:String(location.pathname||'').slice(0,300)});
        })();
    """.trimIndent()
}
'''
BRIDGE.write_text(bridge)

s = EXEC.read_text()
s = s.replace('''import com.oai.geminilivetranslate.ui.AiStudioWebSessionResponseCore\n''','''import com.oai.geminilivetranslate.ui.AiStudioWebSessionResponseCore\nimport com.oai.geminilivetranslate.ui.AiStudioSttPageBridge\n''')
s = s.replace('''    private var activeAttachment: PendingAttachment? = null\n''','''    private var activeAttachment: PendingAttachment? = null\n    private var sttModeModel: String? = null\n''')
needle = '''    fun currentState(): State = state\n\n    fun start(url: String? = null) {'''
replacement = '''    fun currentState(): State = state\n\n    fun startFileTranscribe(modelId: String) {\n        sttModeModel = modelId\n        val url = "https://aistudio.google.com/u/0/prompts/new_chat?model=${Uri.encode(modelId)}"\n        events?.onLog("R28_STT_DIRECT_START", "model=$modelId path=/u/0/prompts/new_chat")\n        start(url)\n    }\n\n    fun start(url: String? = null) {'''
if needle not in s: raise SystemExit('executor start insertion point missing')
s = s.replace(needle, replacement)

old_refresh = '''    fun refreshDiscovery() {\n        if (destroyed || !pageFinished) return\n        val script = "JSON.stringify(window.__AIS_ADAPTIVE_RUNTIME__ ? window.__AIS_ADAPTIVE_RUNTIME__.discover() : ({ok:false,error:'runtime-not-installed'}))"\n        webView.evaluateJavascript(script) { raw ->\n            val decoded = decodeEvalValue(raw)\n            val obj = runCatching { JSONObject(decoded) }.getOrNull()\n            val count = obj?.optInt("candidateCount", 0) ?: 0\n            val readyCount = obj?.optInt("readyCandidateCount", 0) ?: 0\n            val controllerReady = obj?.optBoolean("controllerReady", false) == true\n            events?.onLog("R10_DISCOVERY", decoded.take(8000))\n            if (pending == null) {\n                if (obj?.optBoolean("ok") == true && controllerReady && readyCount > 0) {\n                    setState(State.READY, "ready controllers=$readyCount candidates=$count")\n                } else {\n                    setState(State.WAITING_FOR_CONTROLLER, "waiting for high-confidence controller candidates=$count")\n                }\n            }\n        }\n    }\n'''
new_refresh = '''    fun refreshDiscovery() {\n        if (destroyed || !pageFinished) return\n        val sttModel = sttModeModel\n        if (sttModel != null) {\n            val script = "JSON.stringify(window.__AIS_STT_PAGE__&&window.__AIS_STT_PAGE__.pageState?window.__AIS_STT_PAGE__.pageState(${JSONObject.quote(sttModel)}):({ok:false,error:'stt-page-bridge-not-installed'}))"\n            webView.evaluateJavascript(script) { raw ->\n                val decoded = decodeEvalValue(raw)\n                val obj = runCatching { JSONObject(decoded) }.getOrNull()\n                events?.onLog("R28_STT_PAGE_PROBE", decoded.take(6000))\n                if (pending == null) {\n                    if (obj?.optBoolean("ready") == true) setState(State.READY, "dedicated STT page ready model=$sttModel")\n                    else setState(State.WAITING_FOR_CONTROLLER, "waiting for dedicated STT surface")\n                }\n            }\n            return\n        }\n        val script = "JSON.stringify(window.__AIS_ADAPTIVE_RUNTIME__ ? window.__AIS_ADAPTIVE_RUNTIME__.discover() : ({ok:false,error:'runtime-not-installed'}))"\n        webView.evaluateJavascript(script) { raw ->\n            val decoded = decodeEvalValue(raw)\n            val obj = runCatching { JSONObject(decoded) }.getOrNull()\n            val count = obj?.optInt("candidateCount", 0) ?: 0\n            val readyCount = obj?.optInt("readyCandidateCount", 0) ?: 0\n            val controllerReady = obj?.optBoolean("controllerReady", false) == true\n            events?.onLog("R10_DISCOVERY", decoded.take(8000))\n            if (pending == null) {\n                if (obj?.optBoolean("ok") == true && controllerReady && readyCount > 0) setState(State.READY, "ready controllers=$readyCount candidates=$count")\n                else setState(State.WAITING_FOR_CONTROLLER, "waiting for high-confidence controller candidates=$count")\n            }\n        }\n    }\n'''
if old_refresh not in s: raise SystemExit('refreshDiscovery block missing')
s = s.replace(old_refresh,new_refresh)

attach_insert = '''    fun attachFile(\n'''
idx = s.find(attach_insert)
if idx < 0: raise SystemExit('attachFile missing')
stt_attach = '''    fun attachSttFile(\n        uri: Uri, displayName: String, mimeType: String, size: Long, callback: (Boolean, String) -> Unit,\n    ) {\n        main.post {\n            if (destroyed || !pageFinished || state != State.READY || sttModeModel == null) { callback(false, "NOT_READY"); return@post }\n            if (activeAttachment != null) { callback(false, "ATTACHMENT_BUSY"); return@post }\n            attachmentSeq += 1\n            val item = PendingAttachment(attachmentSeq, uri, displayName.take(260), mimeType.take(180), size, SystemClock.uptimeMillis(), callback, true)\n            activeAttachment = item\n            events?.onLog("R28_STT_FILE_START", "token=${item.token} name=${item.name} mime=${item.mimeType} size=${item.size}")\n            armSttAttachment(item.token, 0)\n        }\n    }\n\n    private fun armSttAttachment(token: Int, attempt: Int) {\n        val item = activeAttachment ?: return\n        if (item.token != token || destroyed) return\n        if (attempt >= 8) { finishAttachment(token, false, "STT_UPLOAD_TARGET_NOT_FOUND"); return }\n        val script = "JSON.stringify(window.__AIS_STT_PAGE__&&window.__AIS_STT_PAGE__.uploadTarget?window.__AIS_STT_PAGE__.uploadTarget():({ok:false,error:'stt-upload-target-not-installed'}))"\n        webView.evaluateJavascript(script) { raw ->\n            if (activeAttachment?.token != token) return@evaluateJavascript\n            val decoded=decodeEvalValue(raw); val obj=runCatching { JSONObject(decoded) }.getOrNull()\n            events?.onLog("R28_STT_UPLOAD_TARGET", "attempt=${attempt+1} ${decoded.take(5000)}")\n            val x=obj?.optDouble("xRatio", Double.NaN) ?: Double.NaN; val y=obj?.optDouble("yRatio", Double.NaN) ?: Double.NaN\n            if (obj?.optBoolean("ok") == true && x.isFinite() && y.isFinite()) {\n                nativeTapController.requestNativeTap(JSONObject().put("xRatio",x).put("yRatio",y).put("tag","STT_UPLOAD").put("role","stt-upload").put("purpose","file-transcribe-upload").toString())\n                main.postDelayed({ pollSttAttachment(token) }, 450L)\n            } else main.postDelayed({ armSttAttachment(token, attempt+1) }, 650L)\n        }\n    }\n\n    private fun pollSttAttachment(token: Int) {\n        val item=activeAttachment ?: return\n        if (item.token != token || destroyed) return\n        if (SystemClock.uptimeMillis()-item.startedAt > ATTACHMENT_TIMEOUT_MS) { finishAttachment(token,false,"STT_ATTACHMENT_TIMEOUT"); return }\n        val script="JSON.stringify(window.__AIS_STT_PAGE__&&window.__AIS_STT_PAGE__.fileState?window.__AIS_STT_PAGE__.fileState(${JSONObject.quote(item.name)}):({ok:false,error:'stt-file-state-not-installed'}))"\n        webView.evaluateJavascript(script) { raw ->\n            if (activeAttachment?.token != token) return@evaluateJavascript\n            val decoded=decodeEvalValue(raw); val obj=runCatching { JSONObject(decoded) }.getOrNull(); val ready=obj?.optBoolean("ready") == true\n            if (ready) { item.readyScans += 1; if (item.readySince==0L) item.readySince=SystemClock.uptimeMillis() } else { item.readyScans=0; item.readySince=0L }\n            events?.onLog("R28_STT_FILE_POLL", "readyScans=${item.readyScans} ${decoded.take(6000)}")\n            if (ready && item.readyScans>=ATTACHMENT_READY_STABLE_SCANS && SystemClock.uptimeMillis()-item.readySince>=ATTACHMENT_READY_SETTLE_MS) finishAttachment(token,true,decoded)\n            else main.postDelayed({ pollSttAttachment(token) },500L)\n        }\n    }\n\n'''
s = s[:idx] + stt_attach + s[idx:]

marker = '''    fun generateAttachmentFileOnlyNative(\n'''
idx = s.find(marker)
if idx < 0: raise SystemExit('generateAttachmentFileOnlyNative missing')
stt_generate = '''    fun generateSttFileNative(callback: (Result) -> Unit): Boolean {\n        if (destroyed || !pageFinished || state != State.READY || pending != null || sttModeModel == null) { callback(Result(ok=false,error="NOT_READY_OR_BUSY")); return false }\n        val request=beginPreparedAttachmentRequest("",callback,"stt-direct-page-file")\n        events?.onLog("R28_STT_AUTO_RUN_ARMED", "seq=${request.seq} model=${sttModeModel} prompt=false")\n        trySttRunSubmit(request.seq,0)\n        return true\n    }\n\n    private fun trySttRunSubmit(requestSeq: Int, attempt: Int) {\n        if (pending?.seq != requestSeq) return\n        if (attempt >= NATIVE_SUBMIT_MAX_RETRIES) { finish(requestSeq,Result(ok=false,error="STT_RUN_NO_CAPTURE")); return }\n        val script="JSON.stringify(window.__AIS_STT_PAGE__&&window.__AIS_STT_PAGE__.runTarget?window.__AIS_STT_PAGE__.runTarget():({ok:false,error:'stt-run-target-not-installed'}))"\n        webView.evaluateJavascript(script) { raw ->\n            if (pending?.seq != requestSeq) return@evaluateJavascript\n            val decoded=decodeEvalValue(raw); val obj=runCatching { JSONObject(decoded) }.getOrNull(); events?.onLog("R28_STT_RUN_TARGET", "attempt=${attempt+1} ${decoded.take(5000)}")\n            val x=obj?.optDouble("xRatio",Double.NaN) ?: Double.NaN; val y=obj?.optDouble("yRatio",Double.NaN) ?: Double.NaN; val base=obj?.optInt("baselineCaptureCount",-1) ?: -1\n            if (obj?.optBoolean("ok") != true || !x.isFinite() || !y.isFinite() || base < 0) { main.postDelayed({trySttRunSubmit(requestSeq,attempt+1)},NATIVE_SUBMIT_RETRY_MS); return@evaluateJavascript }\n            nativeTapController.requestNativeTap(JSONObject().put("xRatio",x).put("yRatio",y).put("tag","STT_RUN").put("role","stt-run").put("purpose","file-transcribe-run").toString())\n            main.postDelayed({ checkGenerateCapture(requestSeq,base,"stt-run-${attempt+1}") { started ->\n                if (pending?.seq != requestSeq) return@checkGenerateCapture\n                if (started) { events?.onLog("R28_STT_RUN_ACK", "seq=$requestSeq attempt=${attempt+1} captureStarted=true"); setState(State.GENERATING,"dedicated STT Run triggered GenerateContent"); pollSttResult(requestSeq,0) }\n                else main.postDelayed({trySttRunSubmit(requestSeq,attempt+1)},NATIVE_SUBMIT_RETRY_MS)\n            } },NATIVE_SUBMIT_ACK_MS)\n        }\n    }\n\n    private fun pollSttResult(requestSeq: Int, attempt: Int) {\n        if (pending?.seq != requestSeq) return\n        val script="JSON.stringify(window.__AIS_STT_PAGE__&&window.__AIS_STT_PAGE__.resultState?window.__AIS_STT_PAGE__.resultState():({ok:false,error:'stt-result-state-not-installed'}))"\n        webView.evaluateJavascript(script) { raw ->\n            if (pending?.seq != requestSeq) return@evaluateJavascript\n            val decoded=decodeEvalValue(raw); val obj=runCatching { JSONObject(decoded) }.getOrNull(); val text=obj?.optString("text").orEmpty(); val chars=text.length\n            events?.onLog("R28_STT_RESULT_POLL", "attempt=$attempt chars=$chars source=${obj?.optString("source").orEmpty()} status=${obj?.optInt("status",-1)} responseChars=${obj?.optInt("responseChars",0)} terminal=${obj?.optBoolean("terminal",false)} elapsedMs=${obj?.optLong("elapsedMs",-1)}")\n            if (text.isNotBlank()) { recordProgress(requestSeq,chars,"stt-dom"); finish(requestSeq,Result(ok=true,status=obj?.optInt("status",200) ?: 200,modelText=text,complete=true,phase="stt-dom-result")); return@evaluateJavascript }\n            main.postDelayed({pollSttResult(requestSeq,attempt+1)}, if(attempt<10) 900L else 1800L)\n        }\n    }\n\n'''
s = s[:idx] + stt_generate + s[idx:]

s = s.replace('''        if (result.ok && result.complete && markerSatisfied) finish(requestSeq, result)\n''','''        if (sttModeModel != null && result.ok && result.complete && result.modelText.isBlank()) {\n            events?.onLog("R28_STT_NETWORK_COMPLETE_EMPTY", "seq=$requestSeq status=${result.status} phase=${result.phase}; waiting for dedicated STT DOM result")\n            return\n        }\n        if (result.ok && result.complete && markerSatisfied) finish(requestSeq, result)\n''')
s = s.replace('''            WebViewCompat.addDocumentStartJavaScript(webView, AiStudioWebSessionR11Support.DOCUMENT_START, setOf(AI_STUDIO_ORIGIN))\n''','''            WebViewCompat.addDocumentStartJavaScript(webView, AiStudioWebSessionR11Support.DOCUMENT_START, setOf(AI_STUDIO_ORIGIN))\n            WebViewCompat.addDocumentStartJavaScript(webView, AiStudioSttPageBridge.DOCUMENT_START, setOf(AI_STUDIO_ORIGIN))\n''')
old_chooser = '''                callback.onReceiveValue(arrayOf(item.uri))\n                val mark = "JSON.stringify(window.__AIS_R11_SUPPORT__&&window.__AIS_R11_SUPPORT__.markFileChooserServed?window.__AIS_R11_SUPPORT__.markFileChooserServed(${JSONObject.quote(item.name)},${JSONObject.quote(item.mimeType)},${item.size}):({ok:false,error:'r11-mark-file-not-installed'}))"\n                this@AiStudioWebSessionExecutor.webView.evaluateJavascript(mark) { raw ->\n                    events?.onLog("R18_ATTACHMENT_URI_SERVED", decodeEvalValue(raw).take(5000))\n                    main.postDelayed({ pollAttachment(item.token) }, 250L)\n                }\n'''
new_chooser = '''                callback.onReceiveValue(arrayOf(item.uri))\n                val stt = sttModeModel != null\n                val mark = if (stt) {\n                    "JSON.stringify((function(){var a=window.__AIS_R11_SUPPORT__&&window.__AIS_R11_SUPPORT__.markFileChooserServed?window.__AIS_R11_SUPPORT__.markFileChooserServed(${JSONObject.quote(item.name)},${JSONObject.quote(item.mimeType)},${item.size}):null;var b=window.__AIS_STT_PAGE__&&window.__AIS_STT_PAGE__.markFileChooserServed?window.__AIS_STT_PAGE__.markFileChooserServed(${JSONObject.quote(item.name)},${JSONObject.quote(item.mimeType)},${item.size}):null;return {ok:true,r11:a,stt:b};})())"\n                } else {\n                    "JSON.stringify(window.__AIS_R11_SUPPORT__&&window.__AIS_R11_SUPPORT__.markFileChooserServed?window.__AIS_R11_SUPPORT__.markFileChooserServed(${JSONObject.quote(item.name)},${JSONObject.quote(item.mimeType)},${item.size}):({ok:false,error:'r11-mark-file-not-installed'}))"\n                }\n                this@AiStudioWebSessionExecutor.webView.evaluateJavascript(mark) { raw ->\n                    events?.onLog(if(stt) "R28_STT_URI_SERVED" else "R18_ATTACHMENT_URI_SERVED", decodeEvalValue(raw).take(5000))\n                    main.postDelayed({ if(stt) pollSttAttachment(item.token) else pollAttachment(item.token) }, 250L)\n                }\n'''
if old_chooser not in s: raise SystemExit('chooser block missing')
s = s.replace(old_chooser,new_chooser)
s = s.replace('''        const val VERSION = "2026-09-05-web-session-r12.5-file-only-transcribe-video-probe"''','''        const val VERSION = "2026-09-05-web-session-r12.6-direct-stt-page"''')
EXEC.write_text(s)

TEST.parent.mkdir(parents=True,exist_ok=True)
TEST.write_text(r'''package com.oai.geminilivetranslate.network

import java.io.File
import kotlin.test.Test
import kotlin.test.assertFalse
import kotlin.test.assertTrue

class AiStudioDirectSttPageSourceTest {
    private val root = File(System.getProperty("user.dir")).parentFile ?: File(".")
    private fun source(path: String) = File(root, "src/main/java/com/oai/geminilivetranslate/$path").readText()

    @Test fun fileTranscribeUsesDedicatedSttPage() {
        val client = source("network/AiStudioFileTranscribeClient.kt")
        val executor = source("core/AiStudioWebSessionExecutor.kt")
        val bridge = source("ui/AiStudioSttPageBridge.kt")
        assertTrue(client.contains("startFileTranscribe(model)"))
        assertTrue(client.contains("attachSttFile"))
        assertTrue(client.contains("generateSttFileNative"))
        assertFalse(client.contains("selectModel(exec)"))
        assertTrue(executor.contains("/u/0/prompts/new_chat?model="))
        assertTrue(executor.contains("AiStudioSttPageBridge.DOCUMENT_START"))
        assertTrue(executor.contains("R28_STT_NETWORK_COMPLETE_EMPTY"))
        assertTrue(bridge.contains("ms-stt-zero-state"))
        assertTrue(bridge.contains("data-test-upload-file-input"))
        assertTrue(bridge.contains("R28_STT_RESULT_STATE"))
    }
}
''')

# Final invariants.
assert 'selectModel(exec)' not in CLIENT.read_text()
assert 'startFileTranscribe(model)' in CLIENT.read_text()
assert 'attachSttFile' in EXEC.read_text()
assert 'generateSttFileNative' in EXEC.read_text()
assert 'AiStudioSttPageBridge.DOCUMENT_START' in EXEC.read_text()
assert '/u/0/prompts/new_chat?model=' in EXEC.read_text()
print('R18.18 direct STT patch invariants OK')
